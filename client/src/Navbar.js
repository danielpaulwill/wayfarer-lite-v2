import React from "react";

function Navbar({ user }) {

  return (
    <div id="navbar">
      <h3>navbar</h3>
      <button className="normalButton">Login</button>
    </div>
  )
};

export default Navbar;